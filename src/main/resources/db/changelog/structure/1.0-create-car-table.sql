CREATE TABLE car (
                         id bigint AUTO_INCREMENT NOT NULL,
                         brand VARCHAR(255) NOT NULL,
                         color VARCHAR(255) NOT NULL,
                         PRIMARY KEY (id)
);
